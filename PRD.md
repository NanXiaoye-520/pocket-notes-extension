# Pocket Notes Desktop

**当前产品版本：V0.1**
**当前阶段：新版桌面应用开发**

## 1. 产品定位

Pocket Notes 是一个独立桌面素材摘录小窗口。

核心目标不是做完整知识库，而是让用户在阅读、看小说、浏览资料时，能够以较低负担快速保存素材。Pocket Notes 负责快速采集；复杂整理、筛选和长期管理交由 Obsidian 完成。

## 2. 核心流程

```text
阅读时快速摘录
→ Pocket Notes
→ 标准 JSON 导出
→ Obsidian 导入
→ 批量整理
→ 长期积累与使用
```

## 3. 产品原则

- 快速采集优先。
- 保持简单，尽量少打断阅读。
- 优先保证稳定和长期可维护。
- 用户最低只需输入素材正文。
- 用户可以简单选择分类。
- 不强迫用户在采集阶段填写出处、标签、内容或笔记。
- 不把 Obsidian 的批量整理、复杂筛选和长期管理功能塞进 Pocket Notes。

## 4. 当前 V0.1 主线

Pocket Notes Desktop V0.1 优先实现：

- 独立桌面应用。
- 单一素材采集主界面，取消旧版“待办 / 已处理”Tab。
- 分类选择，并记住上次选择。
- 当前分类同时决定：
  1. 新素材保存到哪个分类；
  2. 当前列表只显示哪个分类。
- 输入框自动增高。
- `Enter` 保存，`Shift + Enter` 换行。
- 新增素材立即出现在当前分类列表第一条。
- 当前分类内部按最新创建时间倒序显示。
- 单击素材直接编辑。
- 单条删除。
- 局部加粗。
- 本地持久化。
- `Alt + Q` 全局显示或隐藏窗口。
- `Esc` 快速隐藏窗口。
- 点击 `×` 真正退出应用。
- 记住窗口位置、大小和上次分类。
- 标准 JSON 导出。
- Markdown 导出。

以上内容是 V0.1 的产品主线，不代表可以在同一开发轮次全部实现。实际开发必须按 `TODO.md` 分阶段进行，并且每次只执行用户明确指定的一小步。

## 5. 当前暂缓

以下内容暂不作为 V0.1 核心：

- 旧“已处理 / 写作”模块。
- 写作工作台专属功能。
- 完整素材详情页。
- Pocket Notes 内复杂出处整理。
- Pocket Notes 内复杂标签整理。
- 批量标签编辑。
- 复杂组合筛选。
- 标签统计。
- Obsidian 式完整素材工作台。
- 云同步。
- 自动双向同步。
- 系统托盘。
- 自动更新。

## 6. 后续需求方向

以下是后续可能安排的方向，不自动进入当前开发轮次：

- 表格导出。
- 最小表：素材内容与分类。
- 完整表：更多字段。
- 单条素材详情页。
- 出处、标签等增强编辑。
- 旧写作入口未来是否恢复。

## 7. 协议约束

Pocket Notes 与 Obsidian 之间的共同字段、字段含义、默认值、标签底层结构、ID、时间、Markdown、JSON、导入导出、重复判断、冲突和向后兼容规则，统一以项目根目录最新版《素材共同数据协议.md》为唯一正式依据。

本 PRD 只描述 Pocket Notes 的产品定位、范围和开发方向，不得自行重新定义或修改共同数据协议。若产品需求涉及协议尚未定义的共同数据规则，应标记为【协议待确认】，确认协议后再进入实现。
