# Pocket Notes Codex 开发规范

本文件是当前 Pocket Notes 项目使用 Codex 开发时的主开发规范。

## 1. 任务开始前

每次开始开发前必须先完整读取：

1. `AGENTS.md`
2. `PRD.md`
3. `TODO.md`
4. `素材共同数据协议.md`

确认当前任务范围、项目阶段和正式协议后，才可以分析或修改实现。

## 2. 协议优先级

项目根目录最新版《素材共同数据协议.md》是以下内容的唯一正式依据：

- 共同字段
- 字段含义
- 默认值
- 标签底层结构
- ID
- 时间
- Markdown
- JSON
- 导入导出
- 重复判断
- 冲突处理
- 向后兼容

不得根据以下内容自行补全、重新定义或修改协议：

- 旧代码
- `CLAUDE.md`
- 旧 PRD
- 旧 TODO
- 历史实现
- 代码注释

如果正式协议没有定义当前所需规则，停止自行设计，并明确标记为【协议待确认】。

不得修改《素材共同数据协议.md》。协议变更必须由用户在负责协议的对话中明确确认后，另行安排。

## 3. 开发原则

- 每次只开发一个明确任务。
- 只执行用户本轮明确指定的内容。
- 不提前开发 `TODO.md` 中的后续阶段。
- 不擅自增加功能。
- 不修改与本轮任务无关的代码。
- 不进行无关重构。
- 保持代码简单、清晰、可维护。
- 如果需求不明确，不猜测。
- 完成当前任务后停止。
- 等待用户测试或下一步指令。

## 4. 数据保护

即使某个协议字段当前没有 UI，也不得在读取、保存、导出或后续迁移过程中静默丢弃该字段。

## 5. Git 规则

GitHub 仓库：

`https://github.com/NanXiaoye-520/pocket-notes-extension`

本地项目目录：

`D:\VibeCoding\PocketNotes`

开发完成并经用户明确确认后，Codex 可以协助：

- `git status`
- `commit`
- `push`

但必须遵守：

- 未经用户明确确认，不自动 commit。
- 未经用户明确确认，不自动 push。

## 6. 开发完成后必须返回

- 修改了哪些文件。
- 为什么修改。
- 如何验证。
- 验证结果。
- 是否存在已知问题。
- 是否发现协议冲突。
- 是否发现协议未定义事项。

## 7. CLAUDE.md 说明

`CLAUDE.md` 是旧 Claude Code 开发阶段遗留的历史规则文件。

当前使用 Codex 开发时，以 `AGENTS.md` 为主开发规范。不要因为 `CLAUDE.md` 中存在旧规则而恢复旧浏览器插件开发路线。
