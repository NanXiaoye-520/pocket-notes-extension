# Pocket Notes Desktop V0.1 阶段计划

## 执行规则

- 每次只执行用户当前明确指定的一小步。
- 完成当前任务后停止，不自动继续后续阶段。
- 每个阶段完成后先验证，再等待用户确认或下一步指令。
- 不进行无关重构。
- 不擅自增加功能。
- 共同数据规则以项目根目录最新版《素材共同数据协议.md》为唯一正式依据。
- 表格导出不是当前核心链路的前置条件。
- 素材详情页不是当前核心链路的前置条件。
- 出处、标签等增强编辑待核心 MVP 稳定后再安排。
- 旧写作模块当前暂缓。

## 阶段 0｜新版项目基线

- [x] 将《素材共同数据协议.md》纳入 Git 基线。
- [x] 用新版产品定位重写 `PRD.md`。
- [x] 用新版阶段计划重写 `TODO.md`。
- [x] 新建 `AGENTS.md`，作为当前 Codex 项目开发规范。
- [x] 用本地 `browser-v2.2.0` tag 冻结旧浏览器扩展 v2.2.0 基线。

## 阶段 1｜桌面应用空壳

- 建立 Electron 与原生 HTML、CSS、JavaScript 的最小项目结构。
- 实现小型桌面窗口、单实例、正常退出、快速隐藏和全局显示或隐藏。
- 本阶段只建立空壳，不提前开发素材业务功能。

## 阶段 2｜素材数据层

- 依据《素材共同数据协议.md》建立素材数据模型与本地持久化。
- 保证当前没有 UI 的协议字段也不会被静默丢弃。
- 协议未定义的规则标记为【协议待确认】，不自行设计。

## 阶段 3｜快速采集 MVP

- 建立单一素材采集界面。
- 实现分类选择、记住上次分类和按当前分类显示列表。
- 实现自动增高输入框、`Enter` 保存和 `Shift + Enter` 换行。
- 实现最新素材置顶、单击编辑和单条删除。

## 阶段 4｜局部加粗

- 依据正式协议实现素材正文局部加粗。
- 验证 Markdown 标记能够完整保存和往返。

## 阶段 5｜标准 JSON 导出

- 严格按照正式协议实现标准 JSON 导出。
- 验证字段、默认值、时间、Markdown、选项库和未知扩展数据的完整性。

## 阶段 6｜Markdown 导出

- 在不改变正式协议字段含义的前提下实现 Markdown 导出。
- 验证文本与局部加粗标记完整保留。

## 阶段 7｜实际使用测试与问题收集

- 进行真实阅读和素材采集测试。
- 收集影响快速采集、稳定性和数据安全的问题。
- 根据实际问题安排后续小步迭代，不自动扩大产品范围。
